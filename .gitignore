node_modules
.venv
*.har
.DS_Store
.env*
!.env.example
__pycache__
*.pyc

# Third-party clone (AWeirdDev/flights) — installed via setup script
flights/

# Local dumps
/tmp-dump-*.json
.cache

# Python-side disk cache for cash quotes
scripts/.cache/

# Playwright persistent profile — Chromium encrypts cookie values with the
# host OS keyring, so this profile is unreadable on any other machine. Use
# auth.json (committed) for cross-machine portability instead.
.auth-state/
